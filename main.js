// main.js

document.addEventListener("DOMContentLoaded", function () {
  // ページの読み込みが完了したら実行されるコード

  // ここに提供されたHTML内のスクリプトを移動する
  // ...

  // 以下は例として、外部ファイルに分割したJavaScript内で追加できるロジックです
  console.log("External JavaScript file loaded!");
});
